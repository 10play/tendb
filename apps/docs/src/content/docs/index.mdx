---
title: tendb
description: The self-hosted Neon alternative — database branching for Postgres on infrastructure you own, AWS, GCP, Azure, or your laptop's Docker.
template: splash
hero:
  title: Branch your real Postgres in seconds
  tagline: The self-hosted Neon alternative — copy-on-write branches of production-shaped data, on infrastructure you own. AWS, GCP, Azure, or your laptop's Docker.
  image:
    html: |
      <img src="/tendb/console-hero-dark.png" alt="The tendb console — branch list, storage and sync tiles, and platform settings" class="dark-only hero-shot" width="1168" height="432" />
      <img src="/tendb/console-hero-light.png" alt="The tendb console — branch list, storage and sync tiles, and platform settings" class="light-only hero-shot" width="1168" height="432" />
  actions:
    - text: Get started
      link: /tendb/getting-started/quickstart/
      icon: right-arrow
    - text: How it works
      link: /tendb/concepts/architecture/
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

```bash
npx @10play/tendb init             # scaffold the infra into your project
tendb up                           # terraform apply + config wiring
tendb branches create my-feature   # copy-on-write branch DB, ready in ~5s
tendb ci ensure 42 | tail -1       # CI: connection URI as the last stdout line
```

<CardGrid stagger>
  <Card title="Branches in seconds" icon="rocket">
    Every branch is a ZFS copy-on-write clone of your synced data — a real,
    writable Postgres that costs only the pages you change. Create it, break
    it, delete it.
  </Card>
  <Card title="Infrastructure you own" icon="setting">
    `tendb init` scaffolds the Terraform, `tendb up` applies it — one host in
    your AWS, GCP, or Azure account, or Docker on your laptop. No SaaS, no
    control plane; your data never leaves your infrastructure.
  </Card>
  <Card title="CI preview databases" icon="code-branch">
    `tendb ci ensure "$PR" | tail -1` prints a ready connection URI as the last
    line of stdout. Idempotent on re-runs; cleanup on PR close succeeds even
    when the platform is torn down.
  </Card>
  <Card title="Neon-style CLI and console" icon="laptop">
    `neonctl`-style commands plus a local web console: branch tree, SQL
    editor, schema browser, snapshots, per-branch monitoring, and Slack
    alerts.
  </Card>
  <Card title="Locked down by default" icon="approve-check-circle">
    No SSH, no inbound internet. Access rides each platform's native tunnel —
    SSM on AWS, IAP on GCP, Bastion on Azure, loopback locally — and clone
    passwords are derived on demand, stored nowhere.
  </Card>
  <Card title="Any Postgres source" icon="cloud-download">
    Sync from Neon, Aurora, RDS — anything with a `postgres://` URL. The
    source connection string lives in your platform's secret store and is
    read by the host at boot.
  </Card>
</CardGrid>

## Built on DBLab Engine

tendb stands on [DBLab Engine](https://github.com/postgres-ai/database-lab-engine)
by [Postgres.ai](https://postgres.ai) — the open-source engine that does the
heavy lifting of ZFS thin cloning and copy-on-write branch databases. tendb
adds the platform packaging around it: Terraform provisioning for
AWS/GCP/Azure/local, tunnel transports, the CLI, the web console, and the CI
contract.
